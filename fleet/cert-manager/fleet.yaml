# cert-manager/fleet.yaml
defaultNamespace: cert-manager

namespaceLabels:
  cattle.io/os: linux

helm:
  repo: "oci://quay.io/jetstack/charts/cert-manager"
  version: v1.20.3
  values:
    crds:
      enabled: true
